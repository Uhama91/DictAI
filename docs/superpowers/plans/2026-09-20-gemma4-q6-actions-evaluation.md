# Évaluation Q6 sur GitHub Actions

Lot complexe, deux revues du principal avant toute publication de branche ou exécution distante. L'utilisateur a autorisé les Actions et la livraison des nouveaux poids. Les essais locaux Q6 n'ont produit aucune réponse : la mémoire disponible après hachage reste sous 4 Gio. Aucun nouvel essai local ni relèvement des bornes.

## Périmètre

Créer un workflow séparé dans le worktree Android, déclenché au push sur `codex/gemma4-layout-validation` seulement lorsque ses propres fichiers changent. Runner public standard `ubuntu-24.04-arm`, 4 vCPU/16 Go selon la documentation GitHub consultée le 20 septembre 2026 ; pas de runner payant. Conserver le workflow Android existant. La première publication de branche peut déclencher son build historique ; ce build ne sera pas présenté comme le nouvel APK.

Le lot prépare un paquet autonome `tools/gemma4-eval/` contenant le client natif gelé byte-identique (SHA 8e6f0ea79068b354980270ba0bc57d3dd36848d3c04210d0fe464802358719f4), ses deux helpers de prompt et les dépendances Python locales indispensables, la sélection gelée 60 et le valid 196 byte-identiques. Aucun train, holdout, poids, secret ou enregistrement utilisateur dans Git. Vérifier que les exemples exportés sont synthétiques. Le client reste inchangé : utiliser ses arguments explicites pour les chemins et hashes.

Préparer hors Git un tokenizer minimal avec les fichiers de tokenizer/config/template identiques au snapshot BF16, sans shards de poids ni manifeste référençant des fichiers manquants. Prouver localement sur les 60 prompts que texte rendu et identifiants de tokens égalent ceux du snapshot complet. Calculer le nouveau hash de ce répertoire et un manifeste par fichier. Livrer ce petit paquet avec une archive à extraction bornée (noms fixes, aucun lien ni chemin sortant), distinct de l'identité du snapshot complet.

Découper le GGUF Q6 existant `4d8a18db6843337832cebf3f230183241fc3f83ddf5ffbe9f0c798923a6e3cf4`, 3 931 578 880 octets, en parties séquentielles de 1 800 000 000 octets maximum. Prouver hashes, tailles et concaténation. Préparer un manifeste et notices de provenance/licence en s'appuyant sur `reports/gemma4_v7/model_distribution_provenance.md`. Aucun choix définitif pour Android.

Après revue seulement : créer une release DRAFT dédiée `gemma4-v6-1956-q6-evaluation-20260920` après vérification d'absence, ciblant un commit existant, sans rendre le modèle public ni écraser des assets. Le workflow télécharge ses assets via API GitHub authentifiée, en utilisant l'identifiant numérique de la release fixé après création, pas l'endpoint public par tag. La documentation Releases consultée le 20 septembre réserve la visibilité des drafts aux accès push : `contents: write` est donc autorisé uniquement sur ce job déclenché par la branche de travail, sans pull_request ni code de fork. Aucune écriture distante n'est effectuée par le job. `actions/checkout` utilise `persist-credentials: false` ; le token est injecté seulement dans l'étape de téléchargement, jamais dans l'inférence ni ses rapports. Ne jamais journaliser le token. Vérifier les digests/tailles serveur après upload et les hashes locaux après téléchargement.

## Exécution distante bornée

Compiler llama-server au pin `1511ce3bc3f087376c8526b4ad07100bfabb277f`, même transport CPU, 2 threads, contexte 4096, batch/ubatch 128, parallel 1, sans GPU/BLAS/Metal/OpenMP/UI/OpenSSL. Conserver manifestes de build, version système et CPU, commit, commande et SHA du binaire. Python 3.12, transformers 5.17.0 et dépendances réellement utilisées épinglées ; ne pas installer Torch ou MLX si inutiles au tokenizer.

Un wrapper Linux séparé lance le serveur directement, puis le client gelé, attend la disponibilité et conserve journaux/réponses/provenance. Contrôle frais avant spawn : RAM disponible ≥ 8 Gio et disque libre ≥ 7 Gio après assemblage. À l'exécution : RSS serveur ≤ 8 Gio pendant 15 secondes, RAM disponible ≥ 2 Gio, croissance swap ≤ 1 Gio, temps total serveur/client ≤ 45 minutes ; annulation/erreur nettoient uniquement ses enfants. Le job a un timeout 60 minutes. Sur échec, uploader quand même les rapports existants. Refuser les dossiers de résultats déjà présents. Tester timeout, échec de readiness, hash faux, nettoyage, absence de doublon et terminaison normale avec de petits fakes sans E2B.

Exécuter une seule passe sur les 60 exemples gelés. Ce choix de coût fixe est déclaré avant les résultats ; il remplace l'extension locale conditionnée par smoke12. Rapporter séparément les 12 cas déjà définis et les 60, sans choisir les cas après résultat. Linux ARM et macOS diffèrent : la comparaison avec Q4 Mac est descriptive et ne prouve pas une causalité de quantification. Les temps distants ne sont pas des temps téléphone. Aucun score sémantique automatique présenté comme humain.

Le principal examine les résultats avant de sélectionner un modèle ou poursuivre sa distribution publique. Ne pas retoucher prompts, données, client gelé, modèle, guards Android ou poids sur la base de ce lot. Ne pas toucher aux fichiers des autres agents. Préparation, tests et preuves d'abord ; commit/push des seuls fichiers CI explicitement sélectionnés, création du draft et lancement après autorisation d'exécution du principal.

## Correction du transport après le premier lancement

Le run 35521740314 du commit 1bbff69 échoue avant compilation et inférence au téléchargement du manifeste. Les 25 tests distants passent. La lecture du même asset de 802 octets avec `gh api` renvoie le SHA attendu ; le transport Python reçoit 404 après une redirection 302 vers release-assets.githubusercontent.com, avec ou sans conservation du bearer dans le diagnostic contrôlé. L'hypothèse de perte d'autorisation comme cause est donc non confirmée et ne justifie pas de transmettre le bearer au CDN.

Remplacer uniquement le transfert binaire par un sous-processus `gh api` officiel dont stdout est lu en flux, sans seconde copie du modèle. Le token reste dans l'environnement, jamais dans argv ou les rapports ; stderr brut ne doit pas exposer de lien signé. Ajouter une échéance absolue de 300 secondes par asset et nettoyer uniquement ce sous-processus sur timeout ou erreur. Conserver les empreintes, les limites d'inférence et l'évaluation gelée. Tests fakes et contrôle réel limité au manifeste avant deux nouvelles revues, puis reprise Actions autorisée explicitement par le principal. La première passe de génération des 60 exemples reste à effectuer ; aucun résultat Q6 n'existe encore.

## Alignement ARM après le délai atteint

Le run 35523268346 s'arrête au délai prévu : 31 réponses complètes avec EOS, aucune troncature parmi celles-ci, puis interruption du cas suivant. Le journal durable est conservé. Préremplissage médian 79,943 secondes, génération médiane 6,200 secondes ; pic RSS 4 149 149 696 octets, swap nul. Les tests CMake du run indiquent DOTPROD et FP16_VECTOR_ARITHMETIC indisponibles dans le build générique. Le CMake Android sélectionne déjà `armv8-a+dotprod+fp16` pour sa variante adaptée. Ce constat justifie une correction de compilation ; il ne prouve pas à lui seul l'ampleur du gain attendu.

Lot complexe borné : conserver `GGML_NATIVE=OFF` mais fixer `GGML_CPU_ARM_ARCH=armv8-a+dotprod+fp16`, comme cette variante Android. Avant compilation, vérifier sur le runner réel aarch64 que toutes les lignes de capacités CPU exposent `asimd`, `asimddp`, `fphp` et `asimdhp` ; échec explicite sinon, sans repli générique. Conserver cette preuve sans données secrètes. Après compilation, vérifier dans CMakeCache que HAVE_DOTPROD et HAVE_FP16_VECTOR_ARITHMETIC sont vrais. Rapporter l'architecture dans le manifeste de build et inclure les nouveaux scripts/tests dans le manifeste du paquet. Tester la reconnaissance/refus des capacités avec de petites fixtures, sans charger de modèle local.

Ne modifier ni les poids, ni les prompts, ni le client, ni la sélection, ni le décodage, ni les deux threads, ni les limites de ressources et de temps. Aucune modification Android ni nouvelle quantification. Le nouveau run évaluera les mêmes 60 exemples depuis le début dans un répertoire neuf : ne pas mélanger les 31 réponses génériques et les réponses de l'autre configuration. Les 31 restent un diagnostic incomplet, en grande partie issu de la première cohorte, donc sans taux global. Deux revues sur empreintes finales puis autorisation explicite du principal avant commit/push et lancement. Aucun retry automatique après cette nouvelle passe.
